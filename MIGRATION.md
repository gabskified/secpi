# MIGRATION.md — Master Plan

> **📌 Setup snapshot — not a live document.** Accurate as of 2026-07-25 (Phase 1 complete). No Claude Code session or subagent reads this file — every session instruction points to `CLAUDE.md`, `docs/DECISIONS.md`, `docs/STATE.md`, `docs/PROJECT_LOG.md`, `docs/FLAGS.md` instead, and that's where live state actually lives. Sections here that overlapped those files (constraints, risk register, decision queue) have already drifted and won't be corrected — treat this document as historical planning context, not current status. **Parts 0.5 (repo architecture) and 7 (the `src/secpi` refactor plan) are the exceptions** — static, not duplicated elsewhere, and worth a fresh accuracy pass right before Stage 7 (the refactor) actually begins.

**The authoritative document for this migration.** Read this to understand *what* is happening and *why*. Use `START_HERE.md` for the commands to type.

Last updated: 2026-07-24. Supersedes all earlier migration notes.

---

# Part 0 — Document map

Twenty-two files were produced. Here is what each is for, so nothing gets read twice or missed.

### Tier 1 — Read first, every session

| File | Purpose | Who reads it |
|---|---|---|
| `CLAUDE.md` | Auto-loads into every session and subagent. Scope constraints, operating rules, orchestration model, confirmed Methods corrections. | Everyone, automatically |
| `docs/DECISIONS.md` | The seven `D-xx` items awaiting the research lead. The true critical path. | Everyone |
| `docs/STATE.md` | Ownership board. Flag counts, open work by agent, verified code health. | Everyone |

### Tier 2 — Operational

| File | Purpose |
|---|---|
| `START_HERE.md` | PowerShell runbook. Setup commands in order, ECC verification, Python environment. |
| `MIGRATION.md` | This file. Plan, rationale, constraints, refactor design, risk register. |
| `docs/SESSION_PROMPTS.md` | Copy-pasteable prompts for Sessions 1–6. |
| `docs/PROJECT_LOG.md` | Append-only history. Entries 1–2 verbatim, Entry 3 gap placeholder, Entry 4 (migration), extended template. |
| `docs/FLAGS.md` | The 45-flag register. *You supply this* — it's your v2 archive, renamed. |

### Tier 3 — Agent definitions (`.claude/agents/`)

`math-auditor.md` · `deriver.md` · `code-stressor.md` · `editorial-flagger.md` · `editor.md`

Loaded when delegated to. Not read manually.

### Tier 4 — Manuscript working files (`manuscript/sections/`)

`README.md` (order of operations) plus nine section files: `00_title_abstract` · `01_introduction` · `02_methods_2.1_2.2_grid` · `03_methods_2.3_cooling` · `04_methods_2.4_secpi` · `05_methods_2.5_2.6_vv` · `06_results_discussion` · `07_conclusion` · `08_references_appendices`

Each carries frontmatter (status, flags, owner, blockers), that section's confirmed corrections, and a paste marker awaiting the original text.

**Anti-duplication rule:** if these documents ever disagree, `CLAUDE.md` wins on rules, `DECISIONS.md` wins on decisions, `PROJECT_LOG.md` wins on history. Fix the loser; don't maintain both. Two documents drifting out of sync is the exact failure you're migrating away from.

---

# Part 0.5 — Repository architecture

## Full tree

```
secpi/
│
├── CLAUDE.md                       ← auto-loads into every session + subagent
├── START_HERE.md                   ← PowerShell runbook
├── MIGRATION.md                    ← this file, the master plan
├── requirements.txt                ← generated by pip freeze
├── .gitattributes                  ← line-ending policy — create FIRST
├── .gitignore
│
├── .claude/
│   └── agents/                     ← 5 subagent definitions, YAML frontmatter
│       ├── math-auditor.md
│       ├── deriver.md
│       ├── code-stressor.md
│       ├── editorial-flagger.md
│       └── editor.md
│
├── docs/                           ← ALL PROJECT STATE LIVES HERE
│   ├── DECISIONS.md                ← D-01…D-07, research-lead queue
│   ├── STATE.md                    ← ownership board, flag counts, code health
│   ├── PROJECT_LOG.md              ← append-only history + entry template
│   ├── FLAGS.md                    ← 45-flag register (your v2 archive, renamed)
│   ├── SESSION_PROMPTS.md          ← prompts for Sessions 1–6
│   ├── proposals/
│   │   ├── normalization_and_stats.md
│   │   └── DERIVER_DIRECTIVE_p0_provenance.md
│   ├── citations/                  ← one note per resolved Deriver item
│   │   └── (empty — populated by deriver)
│   └── archive/
│       └── FLAGS_v1.pdf            ← superseded, kept for provenance
│
├── manuscript/
│   ├── MCS02_SECPI_original.pdf    ← 🔒 FROZEN. Never edited.
│   └── sections/                   ← the editing workspace
│       ├── README.md               ← order of operations
│       ├── 00_title_abstract.md
│       ├── 01_introduction.md
│       ├── 02_methods_2.1_2.2_grid.md
│       ├── 03_methods_2.3_cooling.md
│       ├── 04_methods_2.4_secpi.md
│       ├── 05_methods_2.5_2.6_vv.md
│       ├── 06_results_discussion.md
│       ├── 07_conclusion.md
│       └── 08_references_appendices.md
│
├── legacy/                         ← 🔒 reference implementations, read-only in practice
│   ├── AuditedCode_1.py            ← current canonical, 3,670 lines
│   ├── LATEST_CODE.md
│   ├── INITIALCODE.md              ← superseded 70×70 pipeline
│   └── archive/                    ← 🔒 D-06 salvage: recovered script iterations
│       ├── MANIFEST.md             ← filename, size, mtime, hash, verdict
│       └── (recovered .py files, never modified)
│
├── src/secpi/                      ← target modular package (Stage 7, later)
│
├── results/                        ← timestamped run dirs, never hand-edited
│   └── run_YYYYMMDD_HHMMSS/
│
├── tests/
│   └── test_reproducibility.py     ← modular output == archived legacy output
│
└── .venv/                          ← gitignored
```

## Provenance zones

Four zones with different rules. Knowing which zone a file is in tells you what you may do to it.

| Zone | Paths | Rule |
|---|---|---|
| **🔒 Frozen** | `manuscript/*.pdf`, `legacy/`, `docs/archive/` | Never edited. Evidence of what the original said and what the original code did. Copy out to modify. |
| **📝 Workspace** | `manuscript/sections/`, `src/`, `tests/` | Freely edited. Git tracks every change. |
| **📓 State** | `docs/*.md` | Append or update per the rules in Part 2.4. `PROJECT_LOG.md` is append-only. Flags are never renumbered. |
| **⚙️ Generated** | `results/`, `requirements.txt` | Produced by execution only. **Never hand-edited** — a hand-edited result is indistinguishable from a fabricated one. |

## Where new files go

| New artifact | Destination | Convention |
|---|---|---|
| Log entry | Append to `docs/PROJECT_LOG.md` | `## Entry [N] — [Role] — [date]` |
| New decision | `docs/DECISIONS.md` | Next `D-xx`, never reused |
| New flag | `docs/FLAGS.md` | Next free number (**#46**), never renumbered |
| Resolved citation | `docs/citations/flag_NN_topic.md` | Full bibliographic detail + quoted basis |
| Pipeline run | `results/run_YYYYMMDD_HHMMSS/` | Seed recorded inside |
| Recovered script | `legacy/archive/` + `MANIFEST.md` row | Original filename preserved |
| Refactored module | `src/secpi/` per Stage 7 tree | — |

## Naming conventions

- Section files: `NN_short_name.md`, numeric prefix preserving reading order
- Run directories: `run_YYYYMMDD_HHMMSS` — sortable, unambiguous
- Citation notes: `flag_NN_topic.md` — greppable by flag number
- Agents: lowercase-hyphenated, matching the `name:` field in their frontmatter

## What is gitignored

```
.venv/
__pycache__/
*.pyc
results/
```

**On `results/`:** ignore the bulk output, but when a run produces numbers that enter the manuscript, commit that specific run directory with `git add -f`. The rule is that every published number traces to a committed run — not that every exploratory run is committed.

## Where the 22 migration files live, at a glance

Three at root (`CLAUDE.md`, `START_HERE.md`, `MIGRATION.md`) · five in `.claude/agents/` · four in `docs/` (plus `FLAGS.md`, which you supply) · ten in `manuscript/sections/`.

---

# Part 1 — Why migrate

## The diagnosis

The Claude Projects setup failed for one structural reason: **the log was advisory, but nothing enforced reading it.**

Each chat had to voluntarily search project knowledge. Cross-chat state lived in files that only updated when a human remembered to re-upload them. The evidence is in your own record:

- **Entry 1 could not locate `AuditedCode_1.py`** and audited a pasted excerpt instead.
- **Entry 1 propagated a wrong normalization baseline (2.94 instead of 0.5882)** for an entire session — a double-applied 5× factor that no one caught until Entry 2 executed the function.
- **Entry 3 does not exist in the log at all**, yet `FLAGS.md` cites it as the sole source for four flag resolutions (#20, #26, #35, #38).

## What Claude Code changes

`CLAUDE.md` loads automatically into every session and every subagent. The log stops being a document someone must remember to search and becomes state they cannot avoid. Git supplies the versioning layer Projects never had — which is also how you prove to a reviewer which code produced which number.

**The subagent orchestration is secondary.** The context-loading guarantee is the whole value.

## What migration does *not* fix

Subagents make the coordination problem *sharper*, not smaller. Each runs in an isolated context and returns only a summary; precision dies at that boundary. A subagent can execute forty seeds, observe exact values, and hand back four sentences.

That is the same failure mode as the 2.94 error and the missing Entry 3. So the log isn't redundant under the new architecture — **the log is the fix for the new architecture's weakness.** Every subagent writes full findings to `PROJECT_LOG.md` *before* returning.

---

# Part 2 — Constraint register

Every constraint governing this work, in one place.

## 2.1 Environment

| Constraint | Detail |
|---|---|
| **Shell** | Windows PowerShell. Not bash. ECC's documentation is almost entirely bash — translate it. |
| **PowerShell version** | Check `$PSVersionTable.PSVersion`. 5.1 does **not** support `&&`; that's PS 7+. Write separate lines. |
| **Execution policy** | `.\.venv\Scripts\Activate.ps1` is blocked by default. `Set-ExecutionPolicy RemoteSigned -Scope CurrentUser`, or fall back to `activate.bat`. |
| **Encoding** | PS 5.1 writes UTF-16LE on plain `>`. Always `Out-File -Encoding utf8` or `Set-Content -Encoding utf8`. |
| **Line endings** | `AuditedCode_1.py` is CRLF. Set `.gitattributes` and `core.autocrlf false` **before** adding files, or diffs fill with phantom whitespace and the reproducibility gate becomes unreadable. |
| **Config path** | `%USERPROFILE%\.claude`, not `~/.claude`. `$HOME` resolves correctly in PowerShell. |
| **Python** | `python`, not `python3`. Five packages: numpy, matplotlib, scipy, tqdm, pandas (soft). |
| **Defensive env vars** | `$env:PYTHONUTF8 = "1"` (Windows defaults to cp1252); `$env:MPLBACKEND = "Agg"` (batch-safe). |

Code verified Windows-safe: zero `plt.show()` calls, no hardcoded path separators, 29 uses of `os.path.join`.

## 2.2 Authority

| Role | May do | May never do |
|---|---|---|
| **Research lead** (you) | Close `D-xx`; authorize scope; accept or reject findings | — |
| **Orchestrator** (main thread) | Read, delegate, synthesize, commit, maintain log and flags | Close a `D-xx`; write manuscript prose; assert an unlogged number |
| **Subagents** (the five) | Investigate, execute, report, log | Decide anything; act outside their brief |

Judgments determining what the paper claims belong to the named authors, who must defend them in peer review. "The tooling chose it" is not a defensible answer.

## 2.3 Editorial scope — granted 2026-07-24

**Authorized: unrestricted structural and rhetorical change.** Restructure, reorder, merge, split, relocate to Supplementary, reframe, discard prose wholesale. The editor does not ask permission for structural decisions.

**Not authorized, and not grantable: any change to the data.**

| Allowed | Never |
|---|---|
| Restructure, reorder, condense, reframe | Change what a number says |
| Rewrite any sentence from scratch | Adjust a value to fit a narrative |
| Move content between sections | Interpolate a missing parameter |
| Cut redundant or weak material | Soften an inconvenient finding, or strengthen a hedged one |
| Reframe a claim to match the evidence | Assert a claim the evidence doesn't support |

Where the honest version is *weaker* — a null result, "verified" not "validated", an unreproducible figure — write the weaker version. That is the correct exercise of this authority, not a failure to use it.

## 2.4 Research integrity

1. **Never invent data.** Fabricated allometric constants have already been found and removed once (`np.random.uniform(0.98, 1.02)` posing as a sensitivity index).
2. **Execution-verified claims only.** Run it, print the numbers, cite the numbers.
3. **Append, never overwrite, the log.**
4. **Never renumber flags.** #1–#45 assigned; next free is **#46**.
5. **Cite sources for provenance claims.** "Standard practice" is not a citation.
6. **The study is synthetic and non-georeferenced.** No field site, no remote sensing, no real Caloocan raster.

**The rule underneath all of them:** if you assert a number, you ran the code that produced it this session, or you cite the log entry where someone else did.

## 2.5 Tooling — ECC

Already installed at user level from your prior project. **Do not reinstall.**

| Do | Don't |
|---|---|
| Verify with `/plugin list ecc@ecc` | Run the full installer on top of a plugin install — the most common way to break ECC |
| Confirm Node.js is installed (`node --version`); ECC's hooks need it | Assume hooks work silently — they fail non-blocking without Node, easy to miss |
| Add stack rules to `~/.claude/rules/` (confirmed location, not `~/.claude/rules/ecc/`) if missing | Assume the plugin-cache mirror under `plugins/cache/ecc/...` is durable — it can be wiped on update |
| `node scripts/ecc.js doctor` then `repair` if duplicated | Reinstall to fix duplication |
| Keep MCPs minimal — disable what's irrelevant | Assume a default MCP server is relevant just because it's enabled |
| Confirm `disabledMcpServers` scope on disk if it matters which projects are affected | Assume an interactive `/mcp` disable is global or trust it without checking |

**Confirmed on this install (`ecc@ecc v2.0.0`, verified from disk, not from generic docs):** 67 agents, 278 skills, no official category taxonomy. One default MCP server, `plugin:ecc:chrome-devtools` (29 tools) — irrelevant to this project, disabled via `/mcp`, confirmed project-scoped in `~/.claude.json`. Skills aren't individually toggled the way MCP servers are; most of the 278 are irrelevant clusters (frameworks, healthcare, Web3, homelab, marketing) that simply sit unused. The clusters actually relevant here: Research/knowledge (Deriver), Testing & TDD (code-stressor), Code review & quality (math-auditor/refactor), and Learning/meta/instincts — the last containing `continuous-learning-v2` specifically, see `START_HERE.md` §0.5.

Worth using: `deep-research`, `search-first`, `exa-search` (Deriver queue) · `documentation-lookup` · `verification-loop`, `eval-harness` (regeneration) · `strategic-compact` (long audits) · `iterative-retrieval` · `python-reviewer` agent · `/ecc:plan` · `/checkpoint`, `/quality-gate`.

Model routing: Sonnet default, `/model opus` for the math audit and the Results rewrite.

## 2.6 Scientific scope — fixed, do not drift

| Element | Value |
|---|---|
| Domain | Synthetic 100 × 100 m, non-georeferenced |
| Coarse grid | 10 × 10 cells @ 10 m — planting units |
| Fine grid | 1 m cells — cooling evaluation |
| Grid generation | Binary CA (Almeida et al. 2002 lineage), morphology presets |
| Land use | P 55–65%, A 25–40%, V 5–10% |
| Optimizer | `AntColonySystemACO`, hand-implemented, `n_runs=5` per k |
| Cooling | Gaussian radial decay `exp(−λ(d/C_D)²)`; `decay_lambda=1.9`, `cca_threshold=1.2`, `competition_k=5.0` |
| Species | 6 Philippine TFTs. Narra = *Pterocarpus indicus*; Akleng-parang = *Albizia lebbeck* (author-corrected) |
| Equity | Zone multipliers 0.5–2.0; percentile classes 1–4 |

---

# Part 3 — What this session found

Four findings, none from reading the manuscript. All from mechanical inspection. **That pattern is itself the most important finding.**

### 3.1 Entry 3 is missing — provenance gap

`FLAGS.md` cites "Project Log Entry 3" as sole source for #20, #26, #35, #38. The log ends at Entry 2. Four resolutions rest on an unopenable citation.

**Recoverable.** #35 and #38 are independently verifiable by execution. #20 and #26 are queue assignments with no analytical content. → Session 3.

### 3.2 Flag #43 — Results §3.1 has no code path 🔴

§3.1 reports evaluating **all 63 subsets** of six TFTs, five trees each. `AuditedCode_1.py` cannot do this:

- No combinatorial class in the inventory
- `itertools` imported at line 11, **never called**
- `AntColonySystemACO` accepts `species_subset` (line 1781), **no caller passes it**
- `main_revised_validation()`'s nine steps contain no sweep

The only combinatorial analyzer anywhere is `ComprehensiveSpeciesAnalysis` in `INITIALCODE.md` — **a different experiment**: 31 combinations of *five* species, `n_trees=10`, 70 × 70 grid.

**Orphaned numbers:** SECPI 4.3916 (rank 3/63), 4.3856 (rank 27/63), marginal deltas 0.6291/0.6283, threshold 3.13, the 28% cliff, the 0.03% diversity result. These are the headline findings and they propagate into both Abstract and Conclusion.

**Classification: POTENTIAL ROADBLOCK.** First since v1. → **D-06.**

### 3.3 Flag #44 — `k` means two different things

§3.1: `k` = species subset size (trees fixed at five). Code and the D-03 Wilcoxon design: `k` = tree count. One symbol, two orthogonal axes. → **D-07.**

### 3.4 Flag #45 — stated software stack is wrong

| Manuscript | Reality |
|---|---|
| ACO "via the **scikit-opt** Python library" (Abstract) | No `sko` import. Hand-implemented. |
| Heatmaps via "Matplotlib and **Seaborn**" (§2.4.2) | No `seaborn` import. matplotlib only. |

A reviewer attempting reproduction installs a library nothing uses. Reproducibility defect.

### Revised counts

25 cleared · 2 deferred · 17 pending · **1 potential roadblock** · 0 severe · **45 total**. Next free: **#46**.

---

# Part 4 — Execution breakdown

## Stage 0 — Verify environment (10 min)

1. `$PSVersionTable.PSVersion` — know whether `&&` works
2. `claude --version` — need v2.1.0+
3. `/plugin list ecc@ecc` — confirm ECC present, **do not reinstall**
4. `node --version` — ECC's hooks (incl. GateGuard) need Node; install via `winget install OpenJS.NodeJS.LTS` if missing, then **fully relaunch Claude Code** (not `--resume`) before continuing
5. Confirm `~/.claude/rules/` has `python.md`, `node.md`, the guardrails file, and a `common/` subfolder — verify with a cold question in a fresh session, not by inspecting file contents yourself
6. `/ecc:instinct-status` — confirm empty; note the practical rule is simply "never run `/ecc:learn` or `/ecc:evolve` here," since extraction is opt-in, not automatic — see `START_HERE.md` §0.5
7. `/mcp` — check each server; disable anything irrelevant, confirm the scope of any disable on disk

Commands in `START_HERE.md` §0.

## Stage 1 — Build repo (20 min)

1. Create directory tree
2. **Set `.gitattributes` and `core.autocrlf false` before adding any files** — non-negotiable ordering
3. Unpack the 22-file bundle
4. Copy in PDF, `AuditedCode_1.py`, `LATEST_CODE.md`, `INITIALCODE.md`, flag archives v1 and v2, normalization proposal
5. Extract PDF text into the nine section files beneath their paste markers

Commands in `START_HERE.md` §1.

**On the PDF:** keep `manuscript/MCS02_SECPI_original.pdf` frozen and never edit it. It's your evidence that the writing was elevated without altering findings. The `sections/*.md` files are the workspace.

## Stage 2 — Python environment (10 min)

1. `python -m venv .venv`
2. Set execution policy, activate
3. `pip install numpy matplotlib scipy tqdm pandas`
4. `pip freeze | Out-File requirements.txt -Encoding utf8`
5. Set `PYTHONUTF8` and `MPLBACKEND`
6. `python -m py_compile legacy\AuditedCode_1.py` — must pass
7. **Baseline commit.** Everything after is auditable.

## Stage 3 — Verify the migration worked (5 min, non-negotiable)

Session 1 prompt. Ask the orchestrator to reconstruct the project from `CLAUDE.md`, `DECISIONS.md`, `STATE.md` **alone** — no other files, no tools.

If it can't name the deliverable, the seven open decisions, the unreviewed sections, and why Results are void, **the context is wrong and you fix it now.** Cheapest possible check on the entire migration.

## Stage 4 — Unblocked work (parallel)

| Track | Session | Blocked by |
|---|---|---|
| **D-06 recovery** | Human: message co-authors. Then Session 4. | Nothing — **start immediately** |
| Flag Results/Discussion/Conclusion | Session 2, `editorial-flagger`, `/model opus` | Nothing |
| Close Entry 3 gap | Session 3, `math-auditor` | Nothing |
| Deriver literature (#9, #20, #26, #30) | Ad-hoc, `deriver` | Nothing |
| Methods corrections | Session 6 partial, `editor` | Nothing for §2.1–2.2, §2.5–2.6 |

## Stage 5 — Decisions, then regeneration

Research lead closes **D-02, D-03, D-07**. Then Session 5 (`code-stressor`):

1. Apply goalposts to `normalize_secpi()` — reporting layer only, ACO still optimizes on raw SECPI
2. Run pipeline under Option B fixed cutoffs, timestamped `results/` dir, record seed
3. Paired Wilcoxon on the **pre-specified** metric only — n=30, report statistic, p, rank-biserial effect size
4. Re-check ceiling against optimizer's actual best; if solutions pin at 5.0, say so

## Stage 6 — Editorial

Session 6, `editor`, Option A + Option B always paired.

Section order by readiness: **02 → 05 → 04** (after D-02) **→ 03** (after #30) **→ 01 → 06/07** (after regeneration) **→ 00 last.**

The Abstract is written last because every number in it is downstream of Results.

## Stage 7 — Refactor (after Results regenerate, not before)

**Do not refactor first.** `AuditedCode_1.py` is currently the only artifact that reproduces the pending Results. Regenerate under Option B with the code as-is, archive that run, *then* refactor and prove the modular version reproduces the archived numbers bit-for-bit.

```
src/secpi/
├── config.py          # dataclasses replacing the inline dict in main_revised_validation()
├── species.py         # SPECIES_DATA, get_dbh(), get_computed_lai()
├── grid.py            # TwoLevelUrbanGrid, CA generation, BFS V-zone carving
├── cooling.py         # CorrectedCoolingModel — Gaussian decay, CCA competition
├── secpi.py           # index computation, reference cutoffs, normalize_secpi()
├── aco.py             # AntColonySystemACO
├── analysis/
│   ├── sensitivity.py     # SensitivityAnalyzer (OAT)
│   ├── robustness.py      # MorphologicalRobustnessValidator
│   ├── scenarios.py       # SuboptimalScenariosGenerator
│   ├── statistics.py      # NEW — Wilcoxon, pending D-03
│   └── combinatorial.py   # NEW — 63-subset sweep, pending D-06
├── reporting/
│   ├── visualizer.py      # EnhancedVisualizer
│   └── interpreter.py     # AutomatedInterpreter
└── pipeline.py        # thin orchestrator
```

**Debt to clear:**
- `AutomatedInterpreter.interpret_k_scenarios` is monkey-patched onto the class after definition → move into the class body
- Full experiment config is a literal dict inside `main_revised_validation()` → promote to `config.py` with provenance comments per constant (cited / author-derived / design choice)
- `SECPI_THEORETICAL_MIN/MAX` become goalpost constants after D-02 — leave both with a supersession comment
- Dead `itertools` import — remove, or use it if D-06 produces `combinatorial.py`
- `interpret_scenario_comparison()` prints `"Difference: SIGNIFICANT"` on hardcoded `|Δ| > 0.1` — **a magnitude label, not a test.** Rename it, and never let its output be described as significance

**Reproducibility gate:** `tests/test_reproducibility.py` asserting the modular pipeline reproduces the archived legacy run from a fixed seed. Nothing in `src/` is authoritative until it passes.

### 🔴 Binding acceptance criterion — species-state isolation (decision **D-12**, Flag **#96**)

**Added 2026-07-28 by `code-stressor` when D-12's fix was applied to `legacy/AuditedCode_1.py`.** D-12 targets *both* the reference implementation and this port. `src/secpi/` was empty at the time, so the port half resolves to the criterion below. **It is binding, not advisory: `src/secpi/analysis/sensitivity.py` must not be written without it, and the port is not accepted until the test below passes against it.**

**The defect being carried forward, so it is not silently reintroduced.** `TreeSpecies.SPECIES_DATA` is a **class attribute**, shared by every instance in the process. `SensitivityAnalyzer._run_single_evaluation` perturbs it to measure parameter sensitivity and, before the fix, never restored it. Measured consequences (Project Log Entries 8 and 12, both by execution):

- the LAI write **compounds geometrically** — six identical evaluations drove Narra 6.07 → 18.12492288, so the routine is not idempotent and `n_samples`' repeats are different models, not repeated samples of one;
- from the first species evaluation onward, *"all other parameters held at baseline"* — the definition of a one-at-a-time sweep — is **false**;
- contamination **escapes the sweep** into any later step in the same process.

**What the port must do:**

1. **Snapshot before any write point**, including the cooling-model construction (building a `TreeSpecies` rewrites every species' `CPA`). A deep copy of the whole dict — cheaper to reason about than a partial copy, and it covers `CPA`, which is a *derived* value stored back into its own source dict.
2. **Snapshot `max_CPA` and `max_LAI` as well.** A dict-level restore is **not sufficient**. `_calculate_cpa_and_normalize()` caches both as **instance** attributes and `get_normalized_cooling_potential()` divides by both — they are live denominators in the cooling term. Leave them alone and the fix looks correct at the dict level and stays wrong at the normalization level.
3. **Restore all three in an unconditional `finally`**, so an exception mid-evaluation still leaves shared state clean. Success-path-only restoration does not satisfy this criterion.
4. **Do not restore by recomputation.** Re-running the normalization over restored data must not be assumed to reproduce the original scalars bit-for-bit. Snapshot and restore them directly.
5. **Restore the dict in place at both levels** — outer mapping and each species' inner mapping — rather than rebinding the class attribute, so that any held reference (e.g. `get_species_params()`, which returns the inner dict itself) sees restored values.

**The better structural fix, if the port is willing to change the design.** The root cause is that mutable species state is *class-level*. If `src/secpi/species.py` makes `SPECIES_DATA` **instance-level** (or frozen, with perturbations applied to a per-evaluation copy), the leak becomes unrepresentable rather than merely guarded, and criteria 1–5 collapse into "pass a modified copy." **That is a design change beyond D-12's authorization and must be put to the research lead before it is taken** — but it is the option that removes the defect class rather than patching this instance of it.

**Test that gates acceptance:** `tests/test_species_data_isolation.py` (added 2026-07-28) already parameterizes the implementation under test via the `SECPI_IMPL` environment variable. Point it at the port; both cases must pass. It asserts bit-identity of `SPECIES_DATA`, `max_CPA`, `max_LAI` and all six normalized cooling potentials across a contaminating evaluation on a **long-lived** `TreeSpecies` instance — long-lived because a fresh instance per evaluation hides the defect by construction.

**Evidence anchors:** pre-fix behaviour is preserved at commit **`87d4528`**; the fix and its verification are Project Log **Entry 12**.

---

# Part 5 — Risk register

| Risk | Severity | Mitigation |
|---|---|---|
| **§3.1 script unrecoverable** (Flag #43) | **Critical** — guts the headline contribution | D-06. Search local machines, Colab, old chats, backups today. If unrecoverable: reconstruct the sweep from `species_subset`, accept that numbers will differ, restate every dependent claim. |
| **No H–D allometrics exist** (Flag #30) | High — invalidates allometric sensitivity | Deriver priority. Fallback: scope allometric sensitivity out with a stated limitation. Decide early, not at submission. |
| **More severe flags in unreviewed sections** | High — three sections, zero coverage | Session 2 first. Expect to go well past #46. |
| Wilcoxon returns non-significant | Medium — weakens a claim | Pre-specified in D-03. Write it descriptively. Acceptable outcome. |
| Goalpost ceiling too low | Low | Re-check against optimizer's best after regeneration; raise if solutions pin at 5.0. |
| Context bloat from ECC | Medium — recreates the original problem | Narrow install. Under 10 MCPs. `strategic-compact`. |
| Docs drift out of sync | Medium | Anti-duplication rule in Part 0. One source of truth per category. |
| Title locked at DOI mint | Low but permanent | D-04 before submission. |

---

# Part 6 — Critical path

```
D-06 answered ──→ §3.1 survives? ──→ Abstract & Conclusion headline claims survive?
                        │
D-02 ─┐                 │
D-03 ─┼──→ regenerate Results ──→ Editor writes Results ──→ Abstract rewrite ──→ EarthArXiv → DOI
D-07 ─┘                 │
D-04 ───────────────────┴─────────────────────────────────────────────────────→ DOI (title permanent)

Deriver track (#9, #20, #26, #30) ──── parallel throughout ────┘
```

## The decision queue

| ID | Decision | Effort | Status |
|---|---|---|---|
| **D-06** | Does the 63-subset script exist? | Phone call | **OPEN — start now** |
| D-02 | Normalization ceiling + framing | One number | OPEN — blocking |
| D-03 | Wilcoxon outcome metric | One of two | OPEN — blocking |
| D-07 | `k` notation collision | One symbol | OPEN — blocking |
| D-04 | Final title | One of two | OPEN |
| D-05 | Keep or delete "Chebyshev space (ℤ²)" | Keep/delete | OPEN |
| D-01 | Option B fixed cutoffs | — | ✅ DECIDED |

**D-06 outranks everything.** D-02, D-03, and D-07 gate how Results are *written*. D-06 determines whether §3.1 — and the Abstract and Conclusion claims resting on it — can exist at all.

It requires no tooling, no setup, and no repo. Message your co-authors and ask: **who ran the 63-subset sweep, and where is that script?**

Do that before Stage 0.
